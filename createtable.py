# import sqlite3
#
# conn = sqlite3.connect('transactions.db')
# cursor = conn.cursor()
# cursor.execute('''CREATE TABLE transaction2 (custid Integer Primary key,ac_type Text,date text,amt Float)''')
# conn.close()
